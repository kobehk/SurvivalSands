{
  "name": "survival-sands",
  "version": "1.0.0",
  "description": "Survival game on a desert island",
  "scripts": {
    "build:items": "python3 scripts/generate_item_map.py",
    "build": "npm run build:items"
  }
}
